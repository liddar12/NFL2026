# UI/UX Bug List — REL15 (2026-08-12)

Prioritized UI/UX findings from the REL15 read-only audit, ordered P0 -> P3.
Scope: Lineup (Rel15 optimizer), Compare (Rel15 head-to-head), the tab bar,
the password gate, and the shared view-header design-system rule. Every row
cites `file:line`. Priorities: P0 blocker, P1 usability/perf, P2 clarity/AA,
P3 polish. No P0/P1 findings surfaced in this pass.

Verified: 11 confirmed, 0 rejected. Counts — P0: 0, P1: 0, P2: 1, P3: 10.

| # | Page | Finding | Priority | Status |
|---|------|---------|----------|--------|
| 1 | Compare | Player name is 16px bold painted in a team tint calibrated only for 3:1 (large text >=18px bold) — at the 16px normal-text threshold it needs 4.5:1 and fails WCAG AA. `.cmp-name` uses `color:teamTint(team)` (compare.js:127; theme.css:2366; tint contract teams.js:6-15, theme.css:364). Primary identity text renders sub-AA at every width. Repro: deep-link `#/compare?a=espn-3139477&b=espn-2330`. Fix: render `.cmp-name` at >=18.66px bold (like slate `.team-ab`) so 3:1 is valid, or use `--ink` for the name and move the tint onto a larger element. | P2 | OPEN — file /home/user/nfl2026/app/views/compare.js:127 |
| 2 | index.html | Six `flex:1 1 0` tabs keep default `min-width:auto` (=min-content) and `.tabbar` has no wrap/overflow (theme.css:249-272). 'Players'/'Parlays' at 13px bold italic are ~55px min-content; six labels + padding exceed a 320px (iPhone SE) viewport, and the fixed bar can neither scroll nor wrap, so the last tab ('Model') clips at the edge. Repro: load app at 320px CSS width. Fix: add `min-width:0` to `.tab` and shrink font/padding at a narrow breakpoint, or let the bar scroll/scale down to 320px. | P3 | OPEN — file /home/user/nfl2026/index.html:51 |
| 3 | Gate | Gate overlay is `role=dialog aria-modal=true` (gate.js:45-47) but nothing traps focus — `body.gate-locked` only sets `overflow:hidden`. The static shell's six tabbar `<a href>` links (index.html:51-58) stay in the DOM and focusable, so Tab past the ENTER button escapes into the app behind the overlay, breaking the aria-modal contract on the first screen. Repro: on a non-secure/locked context, Tab past ENTER with a keyboard. Fix: trap Tab/Shift-Tab within `.gate-card` or set `inert`/`aria-hidden` on the app shell while gate-locked. | P3 | OPEN — file /home/user/nfl2026/app/gate.js:58 |
| 4 | Compare | Two finder inputs share one placeholder and have no accessible name — both are `<input type=search placeholder='Search player…'>` with no `<label>`/`aria-label` (compare.js:119). A screen-reader user tabbing hears 'search edit' twice with no way to tell the A slot from the B slot; placeholders are not a reliable accessible name. Repro: open `#/compare` with no picks, tab with a screen reader. Fix: add `aria-label='Player A'/'Player B'` (or real `<label for>`) keyed off `data-side`. | P3 | OPEN — file /home/user/nfl2026/app/views/compare.js:119 |
| 5 | Compare | Tab-less route deselects every tab — main.js maps `#/compare` with `tab:null` (main.js:83) and `setActiveTab(null)` (main.js:91-97) clears `aria-selected` on all six tabs. Arriving from Lineup's '⚖ Compare' link leaves the `role=tablist` bar with zero selection: AT users hear a tablist with no selected tab, sighted users lose the you-are-here cue, and there is no in-page back affordance. Repro: navigate Lineup -> Compare. Fix: keep the parent (Lineup) tab active on `#/compare`, or give Compare its own tab/`aria-current`. | P3 | OPEN — file /home/user/nfl2026/app/main.js:83 |
| 6 | Compare | Comparing a player to himself is unguarded — nothing dedupes the two picks. Every numeric edge diffs to 0 ('even') and `byeEdge(a,a)` hits the `a===b` branch -> '⚠ same' styled as a warning (compare.js:157-158; theme.css:2382): a bye-conflict alert about one player conflicting with himself. Repro: `#/compare?a=espn-3139477&b=espn-3139477`, or pick the same finder hit on both sides. Fix: detect `a===b` and show a neutral 'same player' state, or block picking the same id on both sides. | P3 | OPEN — file /home/user/nfl2026/app/views/compare.js:157 |
| 7 | Compare | 'change'/swap control is ~16-18px tall — well below the 44px touch minimum. `.cmp-swap` is `font-size:10px; padding:2px 8px` (theme.css:2368; compare.js:129), a ~18px hit area on a 402px phone, hard to tap to swap the player versus the ~44px targets used elsewhere. Repro: on a chosen column, tap 'change' on a phone. Fix: give `.cmp-swap` `min-height:44px` or an enlarged tap-area. | P3 | OPEN — file /home/user/nfl2026/app/views/compare.js:129 |
| 8 | Compare | Winner is encoded only by a horizontal glyph while the phone layout stacks the players vertically. At <=560px `.cmp-grid` collapses to one column (theme.css:2391) so A sits ABOVE B, yet each edge still emits '◀ mag' / '▶ mag' where ◀ = A wins (compare.js:151) — a left/right cue for an above/below relationship. Color can't disambiguate: `.cmp-win--a` and `.cmp-win--b` share `var(--pos-txt)` (theme.css:2380). Per-column values remain visible, limiting harm. Repro: open `#/compare` with two picks at <=560px. Fix: swap to an up/down glyph or label the winning player on the stacked layout, and/or give `--a`/`--b` distinct colors. | P3 | OPEN — file /home/user/nfl2026/app/views/compare.js:151 |
| 9 | Compare | Center edge rail uses a hard-coded `padding-top:62px` that undershoots the real column header. At >=560px the `.cmp-id` header stacks name(16px)+pos(11px)+'change' button(~16px, margin-top 4px) + 8px pad + 1px border + 8px margin, and the column adds 12px top pad — the first metric row lands near 80px, ~18px below where the first edge chip sits (theme.css:2374; compare.js:94). The whole rail reads high against the rows it annotates. Repro: open `#/compare` with two picks at >=560px. Fix: align chips to rows via the grid (place chips in the same grid rows) or add a spacer matching the real header instead of a magic px. | P3 | OPEN — file /home/user/nfl2026/app/views/compare.js:94 |
| 10 | Lineup | Team abbreviation is a 3:1-calibrated tint on 11px text, worse on bench rows. Every optimal/bench row prints the team as `<span style=color:teamTint(r.team)>` inside `.lu-meta` at 11px (theme.css:2339; lineup.js:121,159). The tint is guaranteed 3:1 only at >=18px bold (teams.js:6-15, theme.css:364), so 11px small text needs 4.5:1 and fails; bench rows (`.lu-row--bench`, theme.css:2332) sit on the lighter `--surface-2`, dropping contrast further below the `--surface` the tint was calibrated on. Repro: build a roster in Team, open `#/lineup`. Fix: use `--muted`/`--ink` for the 11px abbrev, or only tint text at the calibrated >=18px bold size. | P3 | OPEN — file /home/user/nfl2026/app/views/lineup.js:121 |
| 11 | Design system | View-header classes have zero CSS — every view emits `<header class='view-head'><h1 class='view-title'>…</h1><span class='view-sub'>…</span></header> (slate/players/model/parlays/team/lineup/compare), but the sole stylesheet app/theme.css defines no rules for these three classes (only `h1,h2,h3,p{margin:0}` at line 83). The title falls back to a browser-default h1 (upright, ~2em) instead of the broadcast heavy-italic-uppercase lockup used elsewhere — an inconsistency on the primary header of every view. Repro: load any view, inspect `.view-title`. Fix: add `.view-title`/`.view-sub` rules in the broadcast type system (italic/uppercase/weight, sizing) matching `.wordmark`/`.p-name`. | P3 | OPEN — file /home/user/nfl2026/app/theme.css:2316 |
